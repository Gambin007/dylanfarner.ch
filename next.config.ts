import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Blendet das Next-Logo unten links aus. Es erscheint ohnehin nur im Dev-Modus.
  devIndicators: false,
};

export default nextConfig;
